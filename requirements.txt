fastapi==0.115.5
uvicorn==0.32.0
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
# vLLM 서버는 별도 설치 및 구동 (pip install vllm)
# 로컬 추론만 시도할 경우: transformers, accelerate, torch (GPU 권장)
transformers==4.45.2
accelerate==0.34.2
# 로컬 웹 UI
gradio==4.44.0
```txt
fastapi==0.115.5
uvicorn==0.32.0
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
# vLLM 서버는 별도 설치 및 구동 (pip install vllm)
# 로컬 추론만 시도할 경우: transformers, accelerate, torch (GPU 권장)
transformers==4.45.2
accelerate==0.34.2